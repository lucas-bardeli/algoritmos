algoritmo "Soma de N° Pares < 1000"

var

   soma, i: inteiro

inicio

      para i <- 2 ate 999 passo 2 faca
           soma <- soma + i
      fimpara
      
      escreval ("O resultado da somma dos números pares é:", soma)

fimalgoritmo
